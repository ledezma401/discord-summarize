import { jest, describe, it, expect } from '@jest/globals';

// Create a simple test suite that passes
describe('p command', () => {
  it('should pass a simple test', () => {
    expect(true).toBe(true);
  });
});
